#!/usr/bin/env zsh
#
### show cheatsheet
### https://github.com/chubin/cheat.sh#usage
source _print-help "$(realpath $0)" "$@"

url="cheat.sh"
for i in "$@"; do
    url="${url}/$i"
done
curl -s "${url}"
