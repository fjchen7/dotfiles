#!/usr/bin/env zsh
#
### Delete a branch locally and remotely
###
### $1 - Branch name.
###
### Examples
###
###   git eliminate branch-name
if [[ $1 =~ ("-h"|"--help") ]]; then
    source print-comment "$(realpath $0)" $1
    exit 0
fi

git branch -D $1
git push origin :$1
