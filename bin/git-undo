#!/usr/bin/env zsh
#
### Undo your last commit, but don't throw away your changes
if [[ $1 =~ ("-h"|"--help") ]]; then
    source print-comment "$(realpath $0)" $1
    exit 0
fi

git reset --soft HEAD^
