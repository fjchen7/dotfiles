% cheatsheet
$ cheatsheet: find ${CHEATSHEETS_ROOT} -type f -name '*.md' -exec basename {} .md \; | sort

# My cheatsheet
glow -s ${CHEATSHEETS_ROOT}/glow/mystyle.json ${CHEATSHEETS_ROOT}/<cheatsheet>.md

# Edit cheatsheet by vscode
find ${CHEATSHEETS_ROOT} -name <cheatsheet>.md -exec code {} +

% configuration
# Edit dotfile by vscode
code ${DOTFILES_ROOT}
