% cheatsheet
$ cheatsheet: fd ".*.md" ${CHEATSHEETS_ROOT} --type f --follow --exec basename {} .md \; | sort --- --preview-window hidden

# My cheatsheet
glow -s ${CHEATSHEETS_ROOT}/glow/mystyle.json ${CHEATSHEETS_ROOT}/<cheatsheet>.md | less

;# Edit cheatsheet by vscode
;find ${CHEATSHEETS_ROOT} -name <cheatsheet>.md -exec code {} +

% configuration
# Edit dotfile by vscode
code ${DOTFILES_ROOT}
