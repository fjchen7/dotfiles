% cheatsheet
$ cheatsheet: ${DOTFILES_BIN_ROOT}/_fd_cheat --- --preview-window hidden

# My cheatsheet
glow -s ${DOTFILES_CHEATSHEETS_ROOT}/glow/mystyle.json ${DOTFILES_CHEATSHEETS_ROOT}/<cheatsheet>.md | less

;# Edit cheatsheet by vscode
;find ${DOTFILES_CHEATSHEETS_ROOT} -name <cheatsheet>.md -exec code {} +

% configuration
# Edit dotfile by vscode
code ${DOTFILES_ROOT}
