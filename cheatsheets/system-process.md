# Process information
## list process
- `pstree -p`

## list process that are listening to port
- `netstat -lnp -t` or `ss -lap -t`: TCP
    - replace `-t` with `-u`: UDP

# performance
Basic
- `top`
